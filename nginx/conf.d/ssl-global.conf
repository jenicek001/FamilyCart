# /etc/nginx/conf.d/ssl-global.conf
# Global SSL configuration that can be included in main http block

# SSL session cache (global, shared across all server blocks)
ssl_session_cache shared:SSL:20m;
ssl_session_timeout 4h;
